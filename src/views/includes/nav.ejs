<nav class="bg-white border-gray-200 dark:bg-gray-900">
    <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
            
      <div class="w-full block w-auto" id="navbar-default">
        <ul class="font-medium flex flex-col bg-transparent rounded-lg md:flex-row justify-between rtl:space-x-reverse md:mt-0 md:border-0">
          <li>
            <a href="/" class="block py-2 px-3 text-white rounded bg-transparent md:p-0" aria-current="page">Agenda</a>
          </li>

          <% if (!user) { %>
          <li>
            <a href="/login" class="block py-2 px-3 text-white rounded bg-transparent md:p-0">Entrar</a>
          </li>
          <% } %>

          <% if (user) { %>
            <li>
              <a href="/logout" class="block py-2 px-3 text-white rounded bg-transparent md:p-0">Sair</a>
            </li>
            <% } %>
        </ul>
      </div>
    </div>
  </nav>
  